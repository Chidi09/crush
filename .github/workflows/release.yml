name: Release Pipeline

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write

jobs:
  build-release:
    name: Build Release Asset (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact_name: crush-linux-amd64
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            artifact_name: crush-linux-arm64
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            artifact_name: crush-windows-amd64.exe
          - target: x86_64-apple-darwin
            os: macos-latest
            artifact_name: crush-macos-amd64
          - target: aarch64-apple-darwin
            os: macos-latest
            artifact_name: crush-macos-arm64

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build Statically Linked Release Binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename and Prepare Assets
        shell: bash
        run: |
          mkdir -p dist
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            cp target/${{ matrix.target }}/release/crush-cli.exe dist/${{ matrix.artifact_name }}
          else
            cp target/${{ matrix.target }}/release/crush-cli dist/${{ matrix.artifact_name }}
            chmod +x dist/${{ matrix.artifact_name }}
          fi

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}

  publish:
    name: Sign & Publish OCI Release
    needs: build-release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Install Cosign
        uses: sigstore/cosign-installer@v3.5.0

      - name: Generate Checksums and Sign Assets
        run: |
          cd dist
          sha256sum * > checksums.txt
          echo "Signing binaries securely using cosign..."
          # cosign sign-blob --yes --output-signature checksums.txt.sig checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/**/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
