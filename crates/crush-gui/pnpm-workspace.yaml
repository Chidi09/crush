# pnpm 10+ reads build-script approvals here (no longer from package.json).
# esbuild (Vite) and tailwind oxide ship native binaries via their install scripts.
allowBuilds:
  esbuild: true
  '@tailwindcss/oxide': true
