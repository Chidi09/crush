[package]
name = "crush-ebpf-progs"
version = "0.1.0"
edition = "2021"

[dependencies]
aya-bpf = { version = "0.1", features = ["async"] }
aya-log-ebpf = "0.1"

[[bin]]
name = "crush-ebpf"
path = "src/main.rs"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
